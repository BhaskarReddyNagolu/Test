
CREATE TABLE UserAccountDetails (
  account_number INTEGER(10) NOT NULL,
  user_name VARCHAR(50) NOT NULL,
  login_id varchar(50)  NOT NULL,
  password varchar(20)  NOT NULL,
  premiumUser varchar(5),
  balace_amount BIGINT
);
